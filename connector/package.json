{
  "name": "ofertasflash-mercadolibre-connector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute ofertasflash-meli --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute ofertasflash-meli --remote --file=./schema.sql"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.26.0"
  }
}
